# Base URL (optional - defaults to playwright.config.ts)
BASE_URL=https://valentinos-magic-beans.click/

# Test environment
TEST_ENV=staging

# Optional: Timeout for tests (ms)
TEST_TIMEOUT=30000

# Optional: Enable debug mode
DEBUG=false
